[pytest]
addopts = --verbose
console_output_style = progress
markers =
    unit: Unit test, no infrastructure or app is required.
    component: Component test, requires running infrastructure.
    integration: Integration test that require all components up and running.
    slow: Slow tests.
